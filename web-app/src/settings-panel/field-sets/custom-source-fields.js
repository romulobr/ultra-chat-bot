const customSourceFields = {
    label: 'Custom Source', id: 'source', fields: [
        {id: 'customSource', label: 'Custom Source Name (blank for default)', type: 'text'}
    ]
};
export default customSourceFields;
